Data Validation Job results:

Environment: <%= environment %>
Start time: <%= start_time.to_s %>


Summary of results:

<%= checks.each_with_index.map{|check,i| "#{i+1}) #{check.result_summary}"}.join("\n\n") %>
<%= "\n\n\n#{'-'*80}\n\n\n" %>
Detailed results:

<%= checks.each_with_index.map{|check,i| "#{i+1}) #{check.result_details}"}.join("\n\n") %>