Data Validation Job results:

Environment: <%= environment %>
Start time: <%= start_time.to_s %>


Summary of results:

<%= checks.map{|check| check.result_summary}.join("\n\n") %>

Detailed results:

<%= checks.map{|check| check.result_details}.join("\n\n") %>